version: "3"
services:
  backend:
    build: .
    image: backend
    ports: 
      - "3000:3000"
    container_name: backend
    environment:
      - MYSQL_DATABASE= int222db
      - MYSQL_USER= int222db
      - MYSQL_ROOT_PASSWORD= int222db
    volumes:
      - ~/backendimg:/src/image_resource
    networks:
       project221:
          
networks:
  default:
    external:
       name: project221
  project221:
    external: true
